Place session audio files in this folder.

Expected pattern:
- one or more interview recordings for the session
- file paths referenced from `session.yaml` relative to the session folder

Example:
- `audio/example_interview_part1.m4a`
