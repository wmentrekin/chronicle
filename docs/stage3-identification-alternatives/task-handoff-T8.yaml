# Contract: task handoff from /implement to developer agents

artifact: task_handoff
feature: "stage3-identification-alternatives"
task_id: "T8"
owner_skill: implement

objective: "Update current-state docs and example inputs so Stage 3 backend selection, participant voice enrollment, benchmark outputs, and the recommendation-only rollout gate match the implemented interfaces."
why_this_matters: "T8 closes the implementation loop by documenting the actual Stage 3 contracts and comparison workflow. The code now supports multiple backends and `benchmark-stage3`, but the published docs still describe the older Ollama-only surface."

inputs:
  requirements: "docs/stage3-identification-alternatives/requirements.yaml"
  plan: "docs/stage3-identification-alternatives/plan.yaml"
  relevant_paths:
    - "docs/stage3-identification-alternatives/implementation-report.yaml"
    - "src/chronicle/cli/stage3.py"
    - "src/chronicle/cli/benchmark.py"
    - "src/chronicle/stage3/service.py"
    - "src/chronicle/stage3/benchmark.py"
    - "src/chronicle/stage3/schemas.py"
    - "docs/stages/stage3-identification.md"
    - "docs/cli.md"
    - "docs/artifacts.md"
    - "examples/inputs/global/participants.yaml"

scope:
  owned:
    - "docs/stages/stage3-identification.md"
    - "docs/cli.md"
    - "docs/artifacts.md"
    - "examples/inputs/global/participants.yaml"
  forbidden:
    - "Changing requirements or implementation scope"
    - "Claiming a new production default backend"
    - "Documenting hosted or remote identification paths"
    - "Editing code or tests"

implementation_notes:
  - "Document `identify --backend` and the supported backend ids."
  - "Document `benchmark-stage3`, its explicit truth-file requirement, JSON/Markdown reports, and the fixed recommendation rule."
  - "Document `voice_references` on participant records as optional enrollment input for SpeechBrain-based backends."
  - "Keep the docs explicit that this phase produces a recommendation only; the production default backend is unchanged."
  - "Keep examples local-first and filesystem-based."

validation_expected:
  - "Doc review against implemented CLI options and Stage 3 artifact fields."
  - "Example participant metadata shows valid `voice_references` list shape."

escalate_if:
  - missing_context
  - conflicting_artifacts
  - product_or_architecture_guess_required
  - scope_expansion_required
